;;;; server.lisp - Server API (stub)

(in-package #:clgrpc.server)

;; Stubs for Phase 1
(defun make-server (&key port)
  "Stub: Create gRPC server (to be implemented in Phase 6)"
  (declare (ignore port))
  nil)

(defun start-server (server)
  "Stub: Start gRPC server (to be implemented in Phase 6)"
  (declare (ignore server))
  nil)

(defun stop-server (server)
  "Stub: Stop gRPC server (to be implemented in Phase 6)"
  (declare (ignore server))
  nil)

(defun register-service (server service-name)
  "Stub: Register service with server (to be implemented in Phase 6)"
  (declare (ignore server service-name))
  nil)
